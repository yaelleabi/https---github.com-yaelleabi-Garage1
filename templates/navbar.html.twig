<style>
  .nav-item {
    margin-right: 15px;
  }

  .dropdown-menu>li>a:hover {
    background-color: var(--light);
    border-top-right-radius: 12px;
    border-bottom-right-radius: 12px;
    transition: 0.01s;
  }
</style>
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">
      <img src="/logo.png" alt="Logo" width="60" height="50">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler"
      aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarToggler">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle btn btn-main fw-bold px-3" href="#" role="button" data-bs-toggle="dropdown"
            aria-expanded="false">
            Administration
          </a>
          <ul class="dropdown-menu">
            {% if is_granted("ROLE_ADMIN") %}
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_register') }}">Créer un employé</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_opening_hour') }}">Horaires d'ouverture</a>
            </li>
            <li>
              <hr class="dropdown-divider">
            </li>
            {% endif %}
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_service_list') }}">Services</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_car_list') }}">Véhicules</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_contact_list') }}">Messages</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_review_list') }}">Avis</a>
            </li>
          </ul>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link btn btn-main px-3" href="tel:0560338166">
            <i class="material-icons">phone</i>
            05 60 33 81 66</a>
        </li>
        <li class="nav-item">
          <a class="nav-link btn btn-main px-3" href="{{ path('app_contact') }}">Contact</a>
        </li>
        <li class="nav-item">
          <a class="nav-link btn btn-main px-3" href="{{path('app_car_list')}}">Véhicules d'occasion</a>
        </li>
        {% endif %}
      </ul>
      <form class="d-grid">
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <a href="{{ path('app_logout') }}" class="btn btn-outline-dark" type="submit">Se déconnecter</a>
        {% else %}
        <a href="{{ path('app_login') }}" class="btn btn-outline-dark" type="submit">Se connecter</a>
        {% endif %}
      </form>
    </div>
  </div>
</nav>